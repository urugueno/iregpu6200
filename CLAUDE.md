# Repository conventions

- Whenever code in this repository is changed, append an entry to `chlog.txt`
  describing what changed and why. Use the format already in the file:
  `[YYYY-MM-DD] branch: summary`.
